{
  "version": 2,
  "builds": [
    {
      "src": "api.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["websites/*.json", "sources/*.json"]
      }
    }
  ],
  "routes": [{"src": "/(.*)", "dest": "api.js"}]
}